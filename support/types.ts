export interface ExperienceRecord {
  title: string;
  role: string;
  dates: string;
  description: string;
  techList: string;
}

export interface CVRecord {
  role: string;
  company: string;
  companyURL: string;
  location: string;
  dates: string;
  activities: string[];
}
